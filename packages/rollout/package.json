{
  "name": "@migrationpilot/rollout",
  "version": "0.1.0",
  "description": "Progressive rollout orchestrator for safe migration deployments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@migrationpilot/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
